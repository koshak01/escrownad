# Rust
/target/
**/*.rs.bk

# Node (if any later)
node_modules/

# Agent internal notes (session backups, peer hashes)
.claude/

# Secrets / local config
.env
.env.*
!.env.example
config.toml
**/etc/local.toml
**/etc/*secret*
pass.txt
.mcp.json

# local tooling that must never be committed
synapse.toml
synapse-bridge.log

# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

deploy/nginx/ssl/

# Foundry (solidity)
contracts/out/
contracts/cache/
broadcast/
bot.secret.txt
*.secret.txt

# internal notes and drafts — not for publication
.private/

# vendor documentation obtained under an invite code — not ours to publish
docs/vendor/
