# escrownad-web — the HTTP endpoint, a thin proxy to ws.

[server]
# Mode: "tcp" | "unix" — EXACTLY one, mutually exclusive.
mode        = "tcp"
socket_path = "/tmp/escrownad_web.sock"
# Dev pair: 4100/4101 (free on the development machine).
http_port   = 4100
