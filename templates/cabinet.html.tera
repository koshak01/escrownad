{% extends "_base.html.tera" %}

{% block title %}My deals · EscrowNad{% endblock %}

{% block content %}
<main class="app-page">
  <div class="app-page__head">
    <div>
      <h1>My deals</h1>
      <p class="lead muted">Offers and buys linked to your wallet.</p>
    </div>
    <a class="ds-btn ds-btn--primary" href="/deals/new/">Add offer</a>
  </div>

  {% if not logged_in %}
  <p class="banner-connect">
    <button type="button" class="ds-btn ds-btn--primary" data-wallet-connect data-redirect-after="/deals/">
      Connect wallet
    </button>
  </p>
  {% elif deals | length == 0 %}
  <p class="muted">No deals yet. <a href="/deals/">Browse offers</a> or <a href="/deals/new/">add one</a>.</p>
  {% else %}
  <div class="offer-grid">
    {% for d in deals %}
    <a class="offer-card" href="/deals/{{ d.del_id }}/">
      <div class="offer-card__top">
        <span class="status status--{{ d.del_status }}">{{ d.del_status }}</span>
        <code>{{ d.resource_kind }}</code>
      </div>
      <h2 class="offer-card__title">{{ d.del_title | default(value="Untitled") }}</h2>
      <p class="offer-card__prefix"><code>{{ d.prefix }}</code></p>
    </a>
    {% endfor %}
  </div>
  {% endif %}
</main>
{% endblock %}
